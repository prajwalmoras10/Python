# guessing game
import random

target_num, guess_num = random.randint(1, 10), 0

while target_num != guess_num:
    guess_num = int(input("Guess a number from 1 to 10 till you get the correct one :"))
    if target_num == guess_num:
        print("You guessed it right!")
